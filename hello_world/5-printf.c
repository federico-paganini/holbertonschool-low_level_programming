#include <stdio.h>

/**
 * main - Prints a message with printf
 * Return: Returns the value 0 (Exit without errors)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
