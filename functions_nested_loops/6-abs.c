#include "main.h"

/**
 * _abs - Prints the absolute value of an integer.
 *
 * @n: Recives a integer to out the absolute value.
 *
 * Return: Returns the absolute value of the int.
 */

int _abs(int n)
{
	return (((n < 0) ? -n : n));
}
